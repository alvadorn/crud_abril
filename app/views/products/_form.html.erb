<%= form_with(model: product, local: true) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
        <% product.errors.full_messages.each do |message| %>
          <li>
            <%= message %>
          </li>
          <% end %>
      </ul>
    </div>
    <% end %>


      <div class="row">

        <div class="form-group col-md-6">
          <%= form.label :name, "Nome"%>
            <%= form.text_field :name, id: :product_name, class: "form-control", placeholder: "Digite o nome" %>
        </div>


        <div class="form-group col-md-6">
          <%= form.label :description, "Descrição" %>
            <%= form.text_field :description, id: :product_description, class: "form-control", placeholder: "Digite a descrição" %>
        </div>
      </div>
      <div class="row">
        <div class="form-group col-md-6">
          <%= form.label :price, "Preço" %>
            <%= form.text_field :price, id: :product_price, class: "form-control", placeholder: "Digite o preço" %>
        </div>
      </div>
      <div class="actions">
        <%= form.submit  "Salvar", class: "btn btn-success"%>
      </div>

      <% end %>