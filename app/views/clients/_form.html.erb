<%= form_with(model: client, local: true) do |form| %>
  <% if client.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(client.errors.count, "error") %> prohibited this client from being saved:</h2>

      <ul>
        <% client.errors.full_messages.each do |message| %>
          <li>
            <%= message %>
          </li>
          <% end %>
      </ul>
    </div>
    <% end %>

      <div class="row">
        <div class="form-group col-md-6">
          <%= form.label :name, "Nome" %>
            <%= form.text_field :name, id: :client_name, class:"form-control", placeholder: "Digite o nome"%>
        </div>


        <div class="form-group col-md-6">
          <%= form.label :email %>
            <%= form.text_field :email, id: :client_email, class:"form-control", placeholder: "Digite o email" %>
        </div>
      </div>
      <div class="row">
        <div class="form-group col-md-6">
          <%= form.label :telephone, "Telefone" %>
            <%= form.text_field :telephone, id: :client_telephone, class: "form-control", placeholder: "Digite o telefone" %>
        </div>
      </div>

      <div class="actions">
        <%= form.submit "Salvar", class: "btn btn-success" %>
      </div>
      <% end %>